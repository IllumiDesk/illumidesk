---
- name: pull end-user notebook image
  docker_image:
    name: "{{ docker_illumidesk_end_user_image }}"
    source: pull

- name: pull grader notebook image
  docker_image:
    name: "{{ docker_illumidesk_grader_image }}"
    source: pull
