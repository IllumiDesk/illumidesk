---
- name: pull base illumidesk/base-notebook image
  docker_image:
    name: "{{ docker_illumidesk_notebook_base_image }}"
    source: pull

- name: pull base illumidesk/instructor-notebook image
  docker_image:
    name: "{{ docker_illumidesk_notebook_instructor_image }}"
    source: pull

- name: pull base illumidesk/learner-notebook image
  docker_image:
    name: "{{ docker_illumidesk_notebook_learner_image }}"
    source: pull

- name: pull base illumidesk/grader-notebook image
  docker_image:
    name: "{{ docker_illumidesk_notebook_grader_image }}"
    source: pull
